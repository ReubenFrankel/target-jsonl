# JSONL Configuration
# Copy this file to .env and fill in your actual values

# Optional: Path to the target output directory
TARGET_JSONL_OUTPUT_DIR=output

# Optional: Overwrite output files atomically
TARGET_JSONL_OVERWRITE=false
